lista_numeros = [1, 2, 3]

print(lista_numeros[0])
print(lista_numeros[1])
print(lista_numeros[2])

# Manipulando os valores da lista, colocando o segundo número como 7

lista_numeros = [1, 2, 3]

lista_numeros[1] = 7

print(lista_numeros[0])
print(lista_numeros[1])
print(lista_numeros[2])

# Lista vazia:
lista_vazia = []

lista_vazia.append("Olá")
lista_vazia.append("Mundo")

print(lista_vazia)

# Extrai o total, o menor e o maior valor da lista
numeros = [10, 9, 8, 7, 6]

print("Total: ", len(numeros))
print("Menor valor: ", min(numeros))
print("Maior valor: ", max(numeros))

# append = Acrescenta um novo item no final da lista
# insert = Insere um novo item na posição dada
# pop = Remove e retorna o item da posição
# sort = Ordena a lista
# reverse = Ordena a lista em ordem reversa
# index = Retorna a posição da primeira ocorrência do item
# count = Retorna o número de ocorrências do item
# remove = Remove a primeira ocorrência do item